import { Lato, Homenaje, Adamina } from "next/font/google";

export const lato = Lato({
  weight: ["900","700","400"],
  subsets: ["latin"],
  display: "swap",
});

export const homenaje = Homenaje({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const adamina = Adamina({
    weight: "400",
    subsets: ["latin"],
    display: "swap",
  });
